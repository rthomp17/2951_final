## How to use this directory

1. Run `shape_generator.py`.
2. Run `shape_encoder.py`.

The generator parameters for the shapes are stored in `shape_parameters_(test|train).(pkl|txt)`.
The VAE encodings are stored in `shape_encodings.pkl`.
The shapes are stored in `shape_objs/`.
The shape images are stored in `shape_images/`.